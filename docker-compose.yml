version: "3.2"
services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: chaos_dev
      POSTGRES_PASSWORD: chaos
      POSTGRES_USER: chaos
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
